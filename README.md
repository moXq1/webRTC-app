## This repository contains a WebRTC application built with Next.js and a WebSocket server using the ws library.

Usage:
Start the WebSocket server:

```bash
npm run start
```

Start the Next.js development server:

```bash
npm run dev
```
